# OUT-1.2: 非功能性约束与架构基线 (Non-Functional Requirements & Constraints)

**文档元数据**
* **主题 (Topic)**: `[客户提供的原始 Topic 名称]`
* **分析负责人 (Agent)**: `[Architect AI Agent]`
* **文档状态**: `[Draft / Confirmed]`
* **生成时间**: `[YYYY-MM-DD]`

---

## 💥 0. 全局看板血脉声明 (Master Context Board Traceability)
> **这是依赖 `[EXT-Master_Context_Board.md]` 大盘看板注入与查杀的信息声明**
* [ ] **强继承声明**：本模版定义的全部 NFR 指标与约束，100% 继承自全局 Master Board 当前状态。
* [ ] **局域拓荒声明**：本次设计在此环节**私自打破/拓宽**了原有基线（如：降低了此链路的延时要求，或引入了新的缓存组件红线），我稍后将对此越权行为反写回并更新全局 Board。

---

## 1. NFR 执行摘要 (Executive Summary of NFRs)
**架构风格定调**: {用一两句话总结本次系统设计在非功能性维度的最高优先级偏好。例如：“本次架构演进必须优先保障【极高可用性】与【数据强一致性】，即使牺牲部分开发时间与服务器成本。”}

---

## 2. 性能与容量基线 (Performance & Scalability)
*量化系统在峰值和正常运转下的水位线要求。所有虚词（如“很快”、“很大”）必须转化为具体的数字基线。*

| 性能指标项 (Metric) | 期望阈值 / 目标 (Target Threshold) | SLA 容忍底线 (SLA Limit) | 备注 / 场景条件 (Condition) |
|---------------------|------------------------------------|--------------------------|-----------------------------|
| **QPS / TPS**       | `[如：日常 1,000，大促峰值 10,000]`| `[跌破 500 时触发熔断降级]`| `[针对核心下单接口]`          |
| **API 响应延时**    | `[P95 < 200ms]`                    | `[P99 < 500ms，超时丢弃]`| `[同步交互场景]`              |
| **并发连接数**      | `[支持 10 万长链接同时在线]`        | `[至少 5 万无损降级]`      | `[针对 WebSocket 消息网关]` |
| **数据增量预估**    | `[每日新增数据 500万条 (约 10GB)]` | `[无]`                     | `[决定是否需提前引入分库分表]`|

---

## 3. 可靠性与可用性 (Reliability & Availability)
*定义系统面对局部故障、极端灾害时的生命力。*

| 可用性约束 (Constraint) | 设定指标 (Value) | 描述依据与保障手段预期 (Description / Expectation) |
|-------------------------|------------------|--------------------------------------------------|
| **系统综合可用性 (HA)** | `[如：99.99%]`   | `[全年不可用时间 < 52分钟。需异地多活或多可用区冗余部署]` |
| **RTO (恢复时间目标)**  | `[如：< 15 分钟]`| `[核心节点宕机后，要求服务在 15 分钟内拉起并对外提供服务]` |
| **RPO (恢复点目标)**    | `[如：0 秒]`     | `[绝对不允许数据丢失。强制要求 DB 同步复制与事务强一致保障]` |

🚨 **拓扑拓绘红线警报**：如涉及容灾/异地多活等网络拓扑/状态机逃逸流转图，执行人除了在 Markdown 中直出 `mermaid` 外，必须同时在 `outputs/diagrams/` 留档对应的 `.drawio` 并留下跨文档超链 `> See: [拓扑图/灾备流转图.drawio]`。

---

## 4. 安全、合规与隐私底线 (Security & Compliance)
| 策略领域 (Domain)  | 约束说明与强制标准 (Mandatory Standards & Constraints) |
|--------------------|--------------------------------------------------------|
| **身份与权限 (IAM)** | `[如：必须接入公司统一的 JWT 单点登录，必须遵循严苛的 RBAC 角色控制]` |
| **数据加密 (Crypto)**| `[如：客户 PII (隐私) 数据落盘必须使用 AES-256 加密，传输层强制 TLS 1.3]` |
| **隐私合规 (Law)**   | `[如：系统必须满足 GDPR（可遗忘权）要求，日志中严禁脱敏前打印用户手机号]` |
| **防重复与防资损**   | `[如：核心扣款流必须支持接口幂等校验，防止用户网络抖动导致的重复扣费]` |

---

## 5. 原有技术栈与基础设施限制 (Tech Stack & Infrastructure Limits)
*列出由于公司历史原因、团队习惯或运维基建导致的客观边界，即“带带着镣铐跳舞”的规则。*

* 📌 **核心语言/框架限制**: `[如：只能使用 Java 21 + Spring Boot 3.x 开发，禁用 Go/Node 引入新栈]`
* 📌 **数据库选型限制**: `[如：全公司关系型数据库仅限使用 MySQL 8.0，严禁私自引入 MongoDB]`
* 📌 **中间件与云原生底座**: `[如：MQ 必须复用集团现有的 RocketMQ 集群；必须基于现有的 Kubernetes 集群发布]`
* 📌 **人力与上线时间期限**: `[如：研发资源仅限 2 人月，要求在 10 月 1 日前必须完成灰度上线]`

---

## 6. 容忍度防波及墙 (Anti-Goals & Out-of-Scope)
*为了防备架构演进的无序蔓延和过度设计，本节点强势辟谣“我们不做什么”。*

* 🚫 **绝对不支持/不扩展的业务域**: `[如：本次高并发改造仅限于 C 端首页聚合，后台运营管理系统（B端）链路完全不包含在此改造性能评估的范畴内]`
* 🚫 **严禁重构波及的老链路黑名单**: `[如：老旧的 /v1/legacypay 支付路由处于封禁维护状态，严禁在本次压测和降级策略中被关联和切流碰触]`
* 🚫 **明确放弃的极值幻想**: `[如：不追求“应对不可预期的秒级 10 万并发洪峰”等伪需求，严格将架构承受上限框死在单机 5000 TPS 内，以杜绝资源的过度冗余采购]`

---

## 7. 【流转断头台】OUT-1.2 对下游架构节点的强制影响映射
*(说明本模版中收集到的各项 NFR “紧箍咒”，将如何作为量化标尺，在下游步骤中直接形成生死判定条件)*

| 当前文档产出项 (OUT-1.2 Content) | 击穿/锁死的下游 SOP 节点 (Downstream Target) | 具体的影响推导与阻断逻辑 (Blast & Blocking Logic) |
|----------------------------------|----------------------------------------------|---------------------------------------------------|
| **性能容量基线 (QPS/长连接/数据量)** | `OUT-3.2/3.3` (候选架构权衡与 ADR) | 方案过滤的**最直接裁决者**。若 QPS>万级，则禁用单体/直连DB 方案，强制触发引入 Redis 缓冲/读写分离集群方案，达不到指标则方案驳回。 |
| **高可用与恢复目标 (RPO/RTO)**   | `OUT-4.2` (数据流设计)、`OUT-5.2` (容灾设计) | 若 RPO 严格要求=0，强制切断任何弱一致性中间件的设计路径，倒逼事务强一致。极严苛可用性将引爆 `OUT-5.2` 环节强制必须含有 Fallback 熔断图纸设计。 |
| **安全与防御红线 (合规/资金幂等)** | `OUT-4.1` (API 契约结构设计)                 | 强势改造业务的 API 切面外形。要求在 `OUT-4.1` 产出中必定挂载 `Idempotent-ID` 头部属性与 Token 声明，否则测试/安全团队在 DoD 出库时将拒绝。 |
| **基础设施围栏 / 技术栈黑名单**   | `OUT-3.1/3.2` (备选组件清单选型)             | 执行“绝对一票否决权 (Veto Power)”。例如，如果全局禁止引入外来新数据库组件，即便是最适合的 NoSQL 方案也在这轮必须因为违规而被无情斩杀剔除。 |
| **极限资源排期与成本压迫**       | `OUT-6.1/6.2` (灰度路由与任务拆分)           | 激进的 Deadline 使得耗时的全链路重构方案不攻自破。要求 `OUT-6.1` 先出急救过渡态和降级门阀，并逼迫将开发任务极速分片切割成能够支持高度并行的 WBS 任务栈。 |

---

## 8. 专家级 DoD 质量自检网关 (Definition of Done)
> **执行人注意：只有完全扫平以下核心门槛，针对 `OUT-1.2` 节点的架构基准资产才能被准许流转放行！如不可打勾，则本节点打回重审重练。**

- [ ] **强数值量化红线**: 核验模版正文中，所有关于性能承载、SLA 和延时的描述框中，是否**完全摒弃了**“系统要非常快”、“要好扩展”等定性含糊词？所有的空栏有且仅有精确的数据阈值和 P99 单位？
- [ ] **全局板溯源合法**: 顶部“全局看板血脉声明”已经选择交代，若在调查中强制调大/越界了技术栈红线（拓荒），当前是否已具备了反向同步更新到 `Master_Context_Board.md` 知识库的准备？
- [ ] **防蔓延铁幕降下**: **Anti-Goals** 隔离区中是否至少抛出了 2 条明确的“不做什么”、“不波及什么老模块”的防线，成功为开发团队剪断不切实际的远期设想负担？
- [ ] **命门贯穿性查杀**: “流转断头台”映射表中定义的所有非功能紧箍咒，是否确保能化为一个具体的参数或组件决策，将在接下来的第 3 阶段方案构思和第 4 阶段技术蓝图中，成为无可争议的阻断入参？
