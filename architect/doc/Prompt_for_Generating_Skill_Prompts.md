# 架构师 Agent Skill 生产母体 (Meta-Prompt for Skill Prompts)

> 💡 **使用说明**：
> 这是一个 **“用来写 Prompt 的 Prompt”**。它提炼了我们之前为你生成 `Task 1.1` 和 `Task 1.2` 专属 Skill Prompt 时的系统底层逻辑与高级架构师工作流（7大神圣步骤、配置文件分离、动作锚定、双轨制图等）。
> 
> 下次当你准备让 `skill-creator` 去构建后续的节点（如 `Task 1.3`、`Task 2.1`）时，**你只需要在下方【👇变量区】填入目标 Task ID，并将灰色引用框内所有的文字发给我（或任意大模型）**。它就会为你吐出一份长篇的、威力巨大的专属 Skill Prompt。然后你再把那份生成的 Prompt 去喂给真正的 `skill-creator` 即可。

---

## 复制以下文段发送给 AI（Meta-Prompt 正文）：

━━━━━━━━━━━━━━━━━━━━━━━━━━━

你现在是一名**资深的高维 AI Agent 架构设计师**。
我目前正在使用一个名为 `skill-creator` 的工具，来批量生产一条长链路的【架构师特工流（Architect AI Agent SOP）】上的各个子节点 Skill 实例。

此刻，我需要你协助我撰写一份用于 **“投喂给 `skill-creator` 的特定大段专属 Prompt”**。
本次我们要构建节点的任务目标是：
** Task 1.3 交付物是 交付物深度及格式契约,你这个任务需要参考 前序任务的输出，例如 task 1.1 和 1.2 ，但是不仅于此，如果不知道位置，可以咨询客户**

为了确保整个架构师特工流派的系统血统纯正、运行不死机且具备外挂配置能力，你帮我撰写的这篇向 `skill-creator` 下达指令的 Prompt 中，**必须结构化、且毫不妥协地包含以下 5 个核心板块**（请你根据当前的特定 Task 目标，在描述这些约束阶段时进行恰当的润色与匹配）：

### 核心规范 0：全局基石档案寻址坐标 (Global Path Directory Binding)
在 Prompt 的最顶层，必须为新 Agent 装配一张“高精度寻址地图”，消除其盲目全盘搜索浪费 Token 的行为。规定以下核心文件及架构纲领的**确切相对坐标路径**，强制指令它在取数和反写时直奔坐标点寻找靶材：
- **全局交付物依赖图谱 (DAG 总纲)**：固定位于 `context/sop/Project_Global_IO_Pipeline_Template.md`
- **各领域 SOP 总控册及网络大图**：位于 `context/sop/` 目录下（如 `Architect SOP.md` 及其匹配的 `IO_Mapping.md`）
- **引擎大脑与活体大盘 (Master Context)**：固定位于 `1_shared_context/Master_Context_Board.md`
- **全局任务依赖注册表 (Registry)**：固定位于 `context/sop/Project_Task_Registry.md`
- **标准交付物基底模板库 (Templates)**：位于 `architect/doc/` 根下（如各阶段的 `OUT-1.1_Template.md` 等）
要求 `skill-creator` 必须严厉警告新 Agent：未来任何针对“大盘”、“DAG图谱”、“SOP流程”、“标准模版”的存取动作，必须基于上述既有路径，严禁凭空幻觉路径或全网盲搜！

### 核心规范 1：必须注入“动作锚定与强制状态点火机制 (Execution Protocol & State Checkpointing)”
在 Prompt 的前部，必须强制命令 `skill-creator` 在新 Skill 的工作流底层植入极其冷酷的动作与状态拦截规范。为彻底消灭 Agent **“跳步暴走 (Runaway)”与“长线任务坍缩”**，你必须写入以下三层强制纪律：
1. **状态机强制打卡 (State Checkpointing)**：要求新 Agent 被唤醒后的第一个动作，必须是利用工具读取并维护其私有工作区中的持久化状态底稿（如 `2_agent_workspaces/[当前任务]/.task_state.md`）。在执行每个 Step 前更新状态为 `[IN_PROGRESS]`，完成后写为 `[DONE]`。若前置 Step 没有 `[DONE]` 标记，绝对禁止跳步！
2. **心智点火定锚印记 (含角色与专属技能唤醒)**：在开始执行接下来的核心任务前，必须在对话流中打印专属 Log 作为运行状态透传。更重要的是，在执行任何动作前，要求 AI 必须**强制读取 Step 2 生成的 required_skills.yaml 宣告给自己的人设，并必须利用这些专用技能与经验去完成本步操作！** 日志必须强制输出如下格式：`▶ [Step {N} 启动确认与心智点火] - 正在扮演的角色与经验池：[读取 Step 2 的专家描述与经验] - 本步必须运用的专属技能：[列出适用的技术] - 本步目标：... - 执行逻辑：...`
3. **物理级硬性暂停锁 (Hard Stop & Yield)**：【🚨致命防线】严禁 Agent 连续执行两个以上的 Step！在完成每一个 Step 的动作并落盘后，必须**强制挂起挂断响应**，并在输出末尾高亮附加：`[🛑 物理硬锁: 已就绪！强制暂停等待人类长官输入批准指令后，方可进入下一 Step]`。除非人类下令通过，否则天塌下来也不能擅自推演生成下一步！

### 核心规范 2：不可篡改的 9 大纯血闭环 SOP 阶段 (The 9-Step Workflow)
你生成的 Prompt 必须明确要求新 Agent 严格按照以下 9 步依次执行。请在撰写时，**将下述骨架包装为紧贴当前 Task 具体要求的话术**：
1. **前置入参摄入与动态看板校验**：【⚠️ 全新加标铁律】第一步绝不允许直接开工！Agent 启动后必须立即读取 `Project_Task_Registry.md`，查询当前 Task ID 对应的 `Prerequisites` 列表中所有任务是否已标记为 `[DONE]`。**若有任一前置任务未完成，必须物理中断并报错，严禁跳步推演！** 校验通过后，然后必须强制要求该 Agent 依据项目架构拓扑总纲 `Project_Global_IO_Pipeline_Template.md` 中的“全局产物节点户口库 (Artifact Registry)”，动态且精准地去提取自己强依赖的前序输出产物（无论是全局大盘 `Master_Context_Board.md` 还是某个特定的 `OUT-[前序任务ID].md` 数据资产及关联录音）。只有在发现该表规定的入参数据有断层，或自身专业领域必填项为空时，才被允许离开沙盒向客户发问。提问前在黑板挂 `[提问中]`，得出结论后反写 `[已决断]` 更新黑板源。不再容忍无硬性入参依据的跨级读取和孤岛发问！

2. **动态专属专家角色与技能装配**：必须明确指令 Agent：这一步产生的清单是专门为后续 **Step 4 到 Step 9** 执行奠基的灵魂！要求 Agent 自行推导并定义：为了极其完美地执行接下来的硬核任务，这名 AI Agent **必须扮演什么级别的顶级专家角色？必须具备并运用哪些专精深度的专业技术、核心技能与行业实战经验？** 将这套“角色技能与经验档案”强制落盘写成 `required_skills.yaml` 配置，作为后续步骤的 AI 执行时强制代入并遵循的载体。
3. **动态兵器/工具装配**：推导所需 MCP 工具或检索武器，写成 `required_tools.yaml` 监控白名单，后续受限调用。
4. **透明化预研与双轨记录制 (Transparent Benchmark Research)**：首先基于上下文先列出《拟定调查清单与搜寻策略》（如搜索方向、设定关键词等），并在终端**强制暂停拦截**，请示客户（User）：“我要去查这些方向，您看是否需要删减或补充？”。待客户审批通过后，严格按照高标准约束去动用工具检索**（绝不允许使用内容农场、过时博客！必须检索专业大厂、近期实效的企业级文献参考）**。最后，必须生成两份文件：一是高浓缩的**调研结论底稿**以启发自己的下一步推导；二是将搜索过哪些 URL、为何抛弃某些检索结果的想法如实记录成**调研历程溯源日志 (`Research_Trace_Log.md`)**（存在工作区中备查）。以此彻底消灭盲搜黑盒，极大降低幻觉 Token 消耗并建立信任！
5. **沉浸式问卷对齐**：拿着行业真经，基于系统现有的 `OUT-[当前任务]_Template.md` 基础模版，生成数据调查问卷。
6. **基于专属问卷的访谈实录与专业纠偏指导**：明确指令 Agent **必须强制加载 Step 5 生成的问卷作为提问大纲**，结合问卷进行深度交流。在访谈中，若是感知到客户提出不切实际或存在逻辑悖论的诉求（例如既要极致性能又要零成本），必须马上动用真正的架构师素养，基于 Step 4 的行业储备主动为其提供具备建设性的技术/商业妥协方案，要求客户做出权衡选择，而非机械地记录。**【绝对禁止】将万字的废话实录丢在自己的私有沙盒里！** 获取的最贴合语料/QA 原音（包含客户最终对妥协方案的确认记录），必须强制落盘至项目共同可见的 `1_shared_context/meeting_records/` 目录下（如 `[Task]_[角色]_QA_Log.md`），以备其他兄弟 Agent 用 RAG 检索上下文。
7. **模版动态进化与强制拦截对齐**：核心转折点！结合访谈得到的真实诉求以及行业权威规范，衍生定制化升级原有底层模版。在沙盒生成 `templates/OUT-[当前任务]_Template_Custom.md`。并将**增加内容、增加的理由及下游任务关联影响**汇报给客户。**强制要求客户同意授权后才能继续。**
8. **双轨纯血成文制图与交付分发**：依循客户审批通过的**全套定制版 Custom 模版**进行融收编档。最高指令：成品的 `OUT` 文档绝不允许留在私有沙盒内，必须输出至全盘共享的 `3_final_outputs/` 大目录下；所有涉图文本必须附加 Markdown 内的 Mermaid 代码块，并且必须在 `3_final_outputs/diagrams/` 生成对应的 `.drawio` XML 源文件维持超链接，同时要求 `.drawio` 架构图必须排版美观、极具人类可读性。
9. **全局 SOP 资产反写与任务状态结算**：作为 Agent 最后职责，必须自动利用代码工具顺序执行：
    1. 修改 `Project_Task_Registry.md`，将当前任务行的 `Status` 变更为 `[DONE]`，并填入当前日期。
    2. 修改 `Architect SOP.md` 和 `Project_Global_IO_Pipeline_Template.md` 固化新进化的模版维度与上下游映射。
    3. 生成结印宣告：展示大盘更新结果，并明确宣告哪些下游 Task ID 现已具备启动条件，避免知识熵增！

### 核心规范 3：三层空间隔离强制拓扑约束 (3-Tier Architecture Rule)
在你输出的 Prompt 结尾，务必强塞一套**“三层空间隔离法物理脚手架”**及对应的 ASCII 树状图。向 `skill-creator` 施压，规定新 Agent 在执行时只能往这三大空间写内容，严禁踩踏与制造信息孤岛：
1. `1_shared_context/` (全局绝对共享区 - 引擎大脑)：要求 Agent 强制从这里读取 `Master_Context_Board.md` 获取大盘，且完成访谈后，必须长篇聊天记录存入它下边的 `meeting_records/` 目录。
2. `2_agent_workspaces/` (私有沙盒执行区 - 各自的秘密推演打稿区)：要求当前 Agent **在自己的专属 Task 子文件夹下（如 `task-1.1-backend/` 下）**建立专属的 `config/` (存放 skills.yaml) 和 `templates/` 和退化的 `phases/`（防污染的纯私密运算算子底稿存放区）。这一带严禁对外共享。
3. `3_final_outputs/` (全服结算交付区 - 对外蓝图区)：要求最后产出的漂亮 `OUT-X.X_XXX.md` 文档与精美图纸绝对不能憋在自己的工作区里，全部汇流至这层共享的对外交付目录下。

### 核心规范 4：出台前的强制自检与合规审查报告 (Pre-Flight Compliance Audit)
在你生成完这份几千字的专属 Skill Prompt 之后，**你必须停下来，对自己刚刚写出的内容进行一次极其苛刻的自省审查！**
因为任何一条指令的遗漏都会导致下游制造出不可控的“暴走 Agent”。
你需要在整个输出的最后，向我呈递一份**《新 Skill 架构合规自查报告》**。这份报告必须逐条以 Check (✅/❌) 的形式核对我前文下达的硬性指标：
- [ ] 开头是否写入了强制读取并维护 `.task_state.md` 的打卡逻辑？
- [ ] 动作前是否加入了大喊 `[读取 required_skills.yaml 里的专家人设]` 的强制日志透传指令？
- [ ] 在每一个动作落盘结束前，是否都高亮加入了红色的**物理硬锁拦截标识** (Hard Stop)？
- [ ] Step 2 是否按要求落实了生成保障 4-9 步的“动态专家角色与技能经历 yaml”设计？
- [ ] Step 4 是否严厉要求了全透明公开去哪里调研，且绝不查内容农场？
- [ ] Step 6 是否告诫了架构妥协会谈的录音必须放进 `1_shared_context/meeting_records/`？
- [ ] 结尾是否框定了这绝不逾矩的三层目录隔离架构？
【最高指令】：如果你自查发现任何一条没做到，直接在后台原地重写，**直到你能交出一份全打勾的纯血合规报告！**

---
请你现在彻底吸收消化以上的生成系统框架准则。立刻为我发出的 **[当前 Task ID]** 写出那份一气呵成、能够直接触发 `skill-creator` 生态的专属底层大段 Prompt 指令文本，并在文末附上自查报告！

━━━━━━━━━━━━━━━━━━━━━━━━━━━
