# 设计方案：为 Architect SOP IO Mapping 补全前端架构支线

**日期**: 2026-04-01
**方案**: 方案 B — 前端专属任务插入式
**目标文件**: `doc/Architect_SOP_IO_Mapping.md`

---

## 背景与问题

现有 SOP 隐性假设「系统 = 后端服务 + 数据库」，前端在架构设计中几乎不可见，唯一的交叉点是 Task 4.1（API 契约），且也是从服务端视角出发。

前端交付失败的常见原因不被当前 SOP 覆盖：
- 状态管理失控
- 组件边界模糊导致牵一发动全身
- 渲染策略选错（SSR/CSR/SSG 误判）
- 构建与部署缺乏规划
- 前端技术债未被识别

---

## 设计方向：方案 B（前端专属任务插入）

在现有 Phase 内部新增前端专属子 Task，并在现有 Task 上追加前端维度。
所有新增 Task 标注触发条件：`🖥️ 含前端交付时执行`，纯后端项目可跳过。

---

## 具体变更设计

### 1. Phase 1 — Task 1.2 扩充：前端 NFR 维度

**位置**: 在现有 `OUT-1.2` 输出项后追加前端专属约束维度。

**追加内容（OUT-1.2 输出扩充）**：
```
前端 NFR 维度（🖥️ 含前端交付时补充）：
- 渲染策略约束（CSR / SSR / SSG / ISR 倾向）
- Core Web Vitals 目标（LCP ≤ ? ms, CLS ≤ ?, INP ≤ ? ms）
- 首屏时间目标（FCP / TTI）
- JS Bundle 体积预算（初始包 ≤ ? KB gzip）
- 无障碍等级（WCAG 2.1 AA / AAA）
- 国际化/本地化 (i18n/l10n) 要求
- 浏览器/设备兼容性矩阵
```

---

### 2. Phase 2 — 新增 Task 2.5：前端现状摸底

**位置**: 紧接 Task 2.4 之后插入，作为 Phase 2 的最后一个任务。

```markdown
### Task 2.5: 前端现状摸底（🖥️ 含前端交付时执行）

*   **输入 (Inputs)**:
    *   `OUT-1.1`: 核心功能点（提供前端页面/交互的业务边界）
    *   `[EXT-Codebase]`: 项目前端代码（页面、组件、路由、store 等）

*   **输出 (Outputs)**:
    *   `OUT-2.5`: 前端 AS-IS 全景分析，包含：
        - **技术栈与构建工具**: 框架版本、Bundler（Webpack/Vite 等）、CI 构建配置
        - **组件树结构**: 页面-组件层级、复用情况、公共 UI 库使用现状
        - **状态管理现状**: 有无集中 Store、数据流是否清晰、是否存在 prop drilling
        - **路由结构**: 页面路由图、权限守卫、懒加载情况
        - **数据请求层**: API Client 封装情况、有无统一错误处理、缓存策略
        - **前端技术债清单**: 巨型组件、循环依赖、裸 fetch、未类型化接口、废弃依赖等
```

---

### 3. Phase 4 — Task 4.1 扩充：前端消费视角

**位置**: 在现有 `OUT-4.1` 输出描述后追加。

**追加内容（OUT-4.1 输出扩充）**：
```
前端消费视角补充（🖥️ 含前端交付时补充）：
- 响应体结构是否对前端渲染友好（避免前端二次转换）
- 统一的错误码与错误消息契约（前端展示/重试逻辑依据）
- 分页/游标规范（前端列表滚动加载设计依据）
- BFF 决策：是否引入 Backend-for-Frontend 层聚合接口
- Loading State 约定：哪些接口需要 Skeleton/Spinner 策略
```

---

### 4. Phase 4 — 新增 Task 4.5：前端架构详设蓝图

**位置**: 紧接 Task 4.4 之后插入，作为 Phase 4 的最后一个任务。

```markdown
### Task 4.5: 前端架构详设蓝图（🖥️ 含前端交付时执行）

*   **输入 (Inputs)**:
    *   `OUT-3.3`: 选定的整体架构方向（决定渲染策略的大框架）
    *   `OUT-2.5`: 前端现状（决定是渐进式改造还是推倒重来）
    *   `OUT-4.1`: API 契约（数据输入格式决定前端 Store/Model 设计）

*   **输出 (Outputs)**:
    *   `OUT-4.5`: 前端架构蓝图，包含：
        - **渲染策略 ADR**: CSR/SSR/SSG/ISR 的选型决策与理由
        - **组件架构设计**: 目录结构规范（原子化/按功能域/按页面）、组件层级划分
        - **状态管理方案**:
            - Global State（Zustand/Redux/Pinia）范围与 Store 设计
            - Server State（React Query/SWR/TanStack Query）缓存策略
            - Form State（RHF/Formik）处理方式
        - **数据请求层设计**: API Client 封装规范、拦截器设计、BFF 接入方案
        - **路由设计**: 页面路由树、懒加载策略、权限守卫实现
        - **构建优化规划**: 代码分割方案、Bundle 体积控制、CDN 资源分离策略
```

---

### 5. Phase 5 — Task 5.3 扩充：前端性能水线

**位置**: 在现有 `OUT-5.3` 输出描述后追加。

**追加内容（OUT-5.3 输出扩充）**：
```
前端性能补充（🖥️ 含前端交付时补充）：
- Core Web Vitals 预估（LCP / CLS / INP 对照 OUT-1.2 NFR 目标）
- 关键路径 JS 体积分析（首屏 Bundle 是否满足预算）
- 渲染性能卡点识别（长列表、频繁 re-render、无防抖的搜索等）
```

---

### 6. Phase 5 — 新增 Task 5.4：前端安全与兼容性

**位置**: 紧接 Task 5.3 之后插入。

```markdown
### Task 5.4: 前端安全与兼容性分析（🖥️ 含前端交付时执行）

*   **输入 (Inputs)**:
    *   `OUT-4.5`: 前端架构蓝图（明确技术选型与渲染策略）
    *   `OUT-1.2`: NFR 清单（兼容性矩阵要求来源）

*   **输出 (Outputs)**:
    *   `OUT-5.4`: 前端安全与兼容性保障方案，包含：
        - **认证安全**: Token 存储策略（Cookie HttpOnly vs localStorage 风险）、刷新机制
        - **XSS 防护**: dangerouslySetInnerHTML / v-html 使用规范、CSP Header 配置
        - **CSRF 防护**: SameSite Cookie 策略、Double Submit Token 机制
        - **内容安全策略 (CSP)**: 生产环境 CSP 规则设计
        - **浏览器兼容性验证**: 对照 OUT-1.2 兼容性矩阵，识别 Polyfill 需求
        - **依赖安全**: 高风险第三方库识别（npm audit 结果分析）
```

---

### 7. Phase 6 — Task 6.1 扩充：前端上线与灰度策略

**位置**: 在现有 `OUT-6.1` 输出描述后追加。

**追加内容（OUT-6.1 输出扩充）**：
```
前端上线策略补充（🖥️ 含前端交付时补充）：
- CDN 部署与缓存失效策略（文件名 Hash vs 手动 Purge）
- 前端 Feature Flag 设计（新旧版本并行期间的功能开关）
- 静态资源回滚方案（快速回退到上一版本的 CDN 路径）
```

---

### 8. Phase 6 — Task 6.2 扩充：前端工作量纳入 WBS

**位置**: 在现有 Task 6.2 的 Input 列表中追加。

**追加输入**：
```
- `OUT-4.5`（前端编码主体工作量：组件、状态管理、路由、构建配置）
- `OUT-5.4`（前端安全加固工作量）
```

---

## OUT-ID 链路总览（前端支线）

```
OUT-1.2(FE NFR)
    └→ OUT-2.5(前端AS-IS)
            └→ OUT-4.5(前端蓝图)
                    ├→ OUT-5.3(FE性能验证)
                    ├→ OUT-5.4(FE安全兼容)
                    └→ OUT-6.1(FE部署策略)
                            └→ OUT-6.2(WBS纳入FE工作量)

OUT-4.1(API契约, 含FE消费视角) → OUT-4.5(前端蓝图输入)
```

---

## 触发规则

| 场景 | 执行 |
|---|---|
| 纯后端/微服务项目 | 跳过所有 🖥️ 标注的任务及扩充内容 |
| 全栈项目（含 Web/移动端前端） | 执行全部 |
| 以前端改造为主、后端不变 | 重点执行 Phase 2 Task 2.5 + Phase 4 Task 4.5 + Phase 5 |

---

## 变更摘要

| 类型 | 位置 | 说明 |
|---|---|---|
| 扩充现有 Task | Task 1.2 | 追加前端 NFR 维度 |
| **新增 Task** | Task 2.5 | 前端现状摸底 |
| 扩充现有 Task | Task 4.1 | 追加前端消费视角 |
| **新增 Task** | Task 4.5 | 前端架构详设蓝图 |
| 扩充现有 Task | Task 5.3 | 追加前端性能指标 |
| **新增 Task** | Task 5.4 | 前端安全与兼容性分析 |
| 扩充现有 Task | Task 6.1 | 追加前端部署/灰度策略 |
| 扩充现有 Task | Task 6.2 | 追加前端工作量输入 |
